spring:
  application:
    name: seat-order
  datasource:
    url: jdbc:mysql://localhost:33060/seat-order?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 666666
  cloud:
    alibaba:
      seata:
        tx-service-group: fsp_tx_group
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
server:
  port: 8082

  consul:
    host: 127.0.0.1
    port: 8500
    config:
      format: yaml
    discovery:
      serviceName: seat-order
      # 健康检查地址使用ip
      prefer-ip-address: true
      # 实例名
      instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}

feign:
  hystrix:
    enabled: false
logging:
  level:
    com.warape.seatorder.mapper: debug
